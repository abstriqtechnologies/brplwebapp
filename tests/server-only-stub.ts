// Stub for "server-only" — Next.js provides this as a build-time guard,
// but tests import the same service code that uses it. The stub is a
// no-op so the test runtime can require server-only modules freely.
export {};